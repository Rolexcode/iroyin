"use client";

import { useEffect, useRef, useState } from "react";
import { useRouter } from "next/navigation";
import { AlertCircle, ArrowRight, BookOpenText, Check, FileAudio, FileCheck2, Languages, LockKeyhole, Mic, Paperclip, RotateCcw, Square, Trash2 } from "lucide-react";
import { createDemoCase } from "@/lib/demo-data";
import { nextClarification } from "@/lib/incident";
import { clearExpiredCases, saveCase } from "@/lib/storage";
import { ACCEPTED_AUDIO_TYPES, LANGUAGE_OPTIONS, MAX_AUDIO_BYTES, MAX_AUDIO_SECONDS } from "@/lib/constants";
import type { ApiErrorBody, IroyinCase, LanguagePair } from "@/lib/types";

type StudioView = "capture" | "processing" | "transcript" | "choose-output" | "result" | "clarify";
type TranscriptProvider = "sahara" | "manual";
type TransformMode = "explain" | "express";
type OutputStyle = "simple_en" | "pcm_en" | "yo_en" | "clear_en" | "academic_en" | "professional_en";
type TranscriptionResult = { state: "complete" | "processing" | "failed"; fileId: string | null; transcript: string | null };
type ReadyTranscript = { text: string; provider: TranscriptProvider; fileId: string | null; originalText?: string };
type TransformResult = { mode: TransformMode; result: string; engine: string; outputStyle?: OutputStyle };

const OUTPUT_OPTIONS: Record<TransformMode, Array<{ value: OutputStyle; label: string; help: string }>> = {
  explain: [
    { value: "simple_en", label: "Simple English", help: "Plain English, with difficult ideas broken down" },
    { value: "pcm_en", label: "Pidgin + English", help: "Explain it in the mix that feels natural" },
    { value: "yo_en", label: "Yorùbá + English", help: "Understand it through a Yorùbá-English mix" },
  ],
  express: [
    { value: "clear_en", label: "Clear English", help: "Clean up the thought without changing the meaning" },
    { value: "academic_en", label: "Academic English", help: "Turn the thought into classroom or assignment language" },
    { value: "professional_en", label: "Professional English", help: "Make it suitable for formal communication" },
  ],
};

function formatBytes(bytes: number) { if (bytes < 1024 * 1024) return `${Math.max(1, Math.round(bytes / 1024))} KB`; return `${(bytes / (1024 * 1024)).toFixed(1)} MB`; }
function errorMessage(payload: unknown, fallback: string) { return (payload as ApiErrorBody | null)?.error?.message ?? fallback; }
async function audioDuration(file: File): Promise<number | null> { return new Promise((resolve) => { const audio = document.createElement("audio"); const url = URL.createObjectURL(file); audio.preload = "metadata"; audio.onloadedmetadata = () => { URL.revokeObjectURL(url); resolve(Number.isFinite(audio.duration) ? audio.duration : null); }; audio.onerror = () => { URL.revokeObjectURL(url); resolve(null); }; audio.src = url; }); }

export function ReporterStudio() {
  const router = useRouter();
  const fileInputRef = useRef<HTMLInputElement>(null);
  const recorderRef = useRef<MediaRecorder | null>(null);
  const streamRef = useRef<MediaStream | null>(null);
  const chunksRef = useRef<Blob[]>([]);
  const recordingTimerRef = useRef<ReturnType<typeof setInterval> | null>(null);
  const [view, setView] = useState<StudioView>("capture");
  const [languagePair, setLanguagePair] = useState<LanguagePair>("pcm_en");
  const [audioFile, setAudioFile] = useState<File | null>(null);
  const [isRecording, setIsRecording] = useState(false);
  const [recordingSeconds, setRecordingSeconds] = useState(0);
  const [processingLabel, setProcessingLabel] = useState("Sending your recording to Sahara…");
  const [readyTranscript, setReadyTranscript] = useState<ReadyTranscript | null>(null);
  const [transcriptDraft, setTranscriptDraft] = useState("");
  const [pendingMode, setPendingMode] = useState<TransformMode | null>(null);
  const [transformResult, setTransformResult] = useState<TransformResult | null>(null);
  const [caseFile, setCaseFile] = useState<IroyinCase | null>(null);
  const [answer, setAnswer] = useState("");
  const [manualOpen, setManualOpen] = useState(false);
  const [manualTranscript, setManualTranscript] = useState("");
  const [error, setError] = useState<string | null>(null);

  useEffect(() => { void clearExpiredCases(); return () => { if (recordingTimerRef.current) clearInterval(recordingTimerRef.current); streamRef.current?.getTracks().forEach((track) => track.stop()); }; }, []);

  const stopRecording = () => { if (recorderRef.current?.state === "recording") recorderRef.current.stop(); if (recordingTimerRef.current) clearInterval(recordingTimerRef.current); recordingTimerRef.current = null; setIsRecording(false); streamRef.current?.getTracks().forEach((track) => track.stop()); streamRef.current = null; };
  const startRecording = async () => { setError(null); if (!navigator.mediaDevices?.getUserMedia || typeof MediaRecorder === "undefined") { setError("This browser cannot record audio here. Upload an audio file instead."); return; } try { const stream = await navigator.mediaDevices.getUserMedia({ audio: true }); const preferredType = ["audio/webm;codecs=opus", "audio/webm", "audio/ogg;codecs=opus"].find((type) => MediaRecorder.isTypeSupported(type)); const recorder = new MediaRecorder(stream, preferredType ? { mimeType: preferredType } : undefined); chunksRef.current = []; streamRef.current = stream; recorderRef.current = recorder; recorder.ondataavailable = (event) => { if (event.data.size) chunksRef.current.push(event.data); }; recorder.onstop = () => { const mimeType = recorder.mimeType || "audio/webm"; const extension = mimeType.includes("ogg") ? "ogg" : "webm"; const blob = new Blob(chunksRef.current, { type: mimeType }); setAudioFile(new File([blob], `iroyin-recording-${Date.now()}.${extension}`, { type: mimeType })); }; recorder.start(500); setRecordingSeconds(0); setIsRecording(true); recordingTimerRef.current = setInterval(() => setRecordingSeconds((current) => { if (current + 1 >= MAX_AUDIO_SECONDS) { queueMicrotask(stopRecording); return MAX_AUDIO_SECONDS; } return current + 1; }), 1000); } catch { setError("Microphone access was not available. Allow access or upload an existing recording."); } };
  const chooseFile = async (file: File | null) => { setError(null); if (!file) return; if (file.size > MAX_AUDIO_BYTES) { setError("That file is larger than 25 MB. Choose a shorter or smaller recording."); return; } if (file.type && !ACCEPTED_AUDIO_TYPES.includes(file.type)) { setError("Use WAV, MP3, MP4, M4A, OGG, WebM, or FLAC audio."); return; } const duration = await audioDuration(file); if (duration && duration > MAX_AUDIO_SECONDS + 0.5) { setError("Recordings must be two minutes or shorter."); return; } setAudioFile(file); };

  const currentTranscript = () => readyTranscript ? { ...readyTranscript, text: transcriptDraft.trim() || readyTranscript.text } : null;
  const finishCase = async (created: IroyinCase) => { await saveCase(created); if (created.missingCriticalFields.length) { setCaseFile(created); setView("clarify"); setProcessingLabel(""); return; } router.push(`/report/${encodeURIComponent(created.caseId)}/review`); };
  const structureTranscript = async (transcript: ReadyTranscript) => { setView("processing"); setProcessingLabel("Finding reportable facts and checking the gaps…"); setError(null); try { const response = await fetch("/api/incidents/extract", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ transcript: transcript.text, languagePair, transcriptProvider: transcript.provider, transcriptFileId: transcript.fileId ?? undefined }) }); const payload = (await response.json()) as { caseFile?: IroyinCase } & ApiErrorBody; if (!response.ok || !payload.caseFile) throw new Error(errorMessage(payload, "The transcript could not be structured.")); await finishCase(payload.caseFile); } catch (caught) { setView("transcript"); setError(caught instanceof Error ? caught.message : "The transcript could not be structured."); } };

  const chooseTransform = (mode: TransformMode) => { const current = currentTranscript(); if (current) setReadyTranscript(current); setPendingMode(mode); setError(null); setView("choose-output"); };
  const transformTranscript = async (mode: TransformMode, outputStyle: OutputStyle) => { if (!readyTranscript) return; const text = transcriptDraft.trim() || readyTranscript.text; setError(null); setView("processing"); setProcessingLabel(mode === "explain" ? "Making the meaning easier to follow…" : "Turning your thought into the form you chose…"); try { const response = await fetch("/api/transform", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ text, mode, languagePair, outputStyle }) }); const payload = (await response.json()) as Partial<TransformResult> & ApiErrorBody; if (!response.ok || !payload.result || !payload.mode || !payload.engine) throw new Error(errorMessage(payload, "Ìròyìn could not transform that text.")); setTransformResult({ ...(payload as TransformResult), outputStyle }); setView("result"); setProcessingLabel(""); } catch (caught) { setView("choose-output"); setError(caught instanceof Error ? caught.message : "Ìròyìn could not transform that text."); } };

  const pollTranscription = async (fileId: string): Promise<TranscriptionResult> => { for (let attempt = 0; attempt < 45; attempt += 1) { await new Promise((resolve) => setTimeout(resolve, 2_000)); const response = await fetch(`/api/transcriptions/status?fileId=${encodeURIComponent(fileId)}`, { cache: "no-store" }); const payload = (await response.json()) as TranscriptionResult & ApiErrorBody; if (!response.ok && response.status !== 202) throw new Error(errorMessage(payload, "Could not check the transcript status.")); if (payload.state !== "processing") return payload; setProcessingLabel(`Sahara is still listening… ${attempt + 1}`); } throw new Error("Sahara is taking longer than expected. Please try again in a moment."); };
  const submitAudio = async () => { if (!audioFile) return; setError(null); setView("processing"); setProcessingLabel("Sending your recording to Sahara…"); try { const form = new FormData(); form.append("audio", audioFile); form.append("languagePair", languagePair); const response = await fetch("/api/transcriptions", { method: "POST", body: form }); let payload = (await response.json()) as TranscriptionResult & Partial<ApiErrorBody>; if (!response.ok && response.status !== 202) throw new Error(errorMessage(payload, "Sahara could not process this recording.")); if (payload.state === "processing") { if (!payload.fileId) throw new Error("Sahara queued the recording without a file reference."); payload = await pollTranscription(payload.fileId); } if (payload.state !== "complete" || !payload.transcript) throw new Error("Sahara did not return a usable transcript."); const original = payload.transcript; setReadyTranscript({ text: original, originalText: original, provider: "sahara", fileId: payload.fileId }); setTranscriptDraft(original); setView("transcript"); setProcessingLabel(""); } catch (caught) { setView("capture"); setError(caught instanceof Error ? caught.message : "Something went wrong while processing the recording."); } };
  const submitManualTranscript = () => { if (manualTranscript.trim().length < 3) { setError("Enter a short transcript before continuing."); return; } const text = manualTranscript.trim(); setError(null); setReadyTranscript({ text, originalText: text, provider: "manual", fileId: null }); setTranscriptDraft(text); setView("transcript"); };
  const resetCapture = () => { setReadyTranscript(null); setTranscriptDraft(""); setPendingMode(null); setTransformResult(null); setCaseFile(null); setAudioFile(null); setError(null); setView("capture"); };
  const loadDemo = async () => { setError(null); const demo = createDemoCase(); await saveCase(demo); setCaseFile(demo); setView("clarify"); };
  const submitClarification = async () => { if (!caseFile || !answer.trim()) return; const clarification = nextClarification(caseFile); if (!clarification) { router.push(`/report/${encodeURIComponent(caseFile.caseId)}/review`); return; } setError(null); try { const response = await fetch("/api/incidents/clarify", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ caseFile, field: clarification.field, question: clarification.question, answer }) }); const payload = (await response.json()) as { caseFile?: IroyinCase } & ApiErrorBody; if (!response.ok || !payload.caseFile) throw new Error(errorMessage(payload, "That detail could not be saved.")); await saveCase(payload.caseFile); setAnswer(""); setCaseFile(payload.caseFile); if (!nextClarification(payload.caseFile)) router.push(`/report/${encodeURIComponent(payload.caseFile.caseId)}/review`); } catch (caught) { setError(caught instanceof Error ? caught.message : "That detail could not be saved."); } };

  const clarification = caseFile ? nextClarification(caseFile) : null;
  const activeStep = view === "capture" ? 1 : view === "processing" ? 2 : view === "transcript" || view === "choose-output" ? 3 : 4;
  const outputOptions = pendingMode ? OUTPUT_OPTIONS[pendingMode] : [];
  const transcriptChanged = Boolean(readyTranscript && transcriptDraft.trim() && transcriptDraft.trim() !== (readyTranscript.originalText ?? readyTranscript.text).trim());

  return <section className="studio" aria-label="Understand code-switched speech" aria-live="polite">
    <div className="studio-header"><div><p className="studio-title">Ìròyìn Voice</p><span className="studio-subtitle">Powered by Sahara speech recognition</span></div><span className={`status-pill ${caseFile?.transcriptProvider === "demo" ? "demo" : ""}`}><span className="status-dot" /> {caseFile?.transcriptProvider === "demo" ? "Demo" : "Live"}</span></div>
    <div className="studio-body">
      <div className="studio-progress" aria-label={`Step ${activeStep} of 4`}>{[1,2,3,4].map((step) => <span key={step} className={`progress-step ${step <= activeStep ? "active" : ""}`} />)}</div>
      {view === "processing" && <div className="processing"><div><div className="processing-ring" aria-hidden="true" /><h2>Working with your words</h2><p>{processingLabel}</p></div></div>}

      {view === "capture" && <><p className="section-kicker">Start with your voice</p><h2>No need to translate yourself first.</h2><p className="studio-intro">Speak naturally in the language mix that comes to you. You decide what Ìròyìn does after Sahara transcribes it.</p><label className="field-label" htmlFor="language-pair">Speech mix</label><select id="language-pair" className="select" value={languagePair} onChange={(event) => setLanguagePair(event.target.value as LanguagePair)}>{LANGUAGE_OPTIONS.map((option) => <option value={option.value} key={option.value}>{option.label}</option>)}</select><div className="capture-area"><button className={`mic-button ${isRecording ? "recording" : ""}`} onClick={isRecording ? stopRecording : startRecording} type="button" aria-label={isRecording ? "Stop recording" : "Start recording"}>{isRecording ? <Square size={23} fill="currentColor" /> : <Mic size={27} />}</button><p className="capture-title">{isRecording ? `Recording · ${recordingSeconds}s` : "Tap to speak"}</p><p className="capture-help">Up to 2 minutes</p></div><div className="or-divider">or</div><input ref={fileInputRef} className="sr-only" type="file" accept="audio/*,.wav,.mp3,.m4a,.ogg,.webm,.flac" onChange={(event) => void chooseFile(event.target.files?.[0] ?? null)} />{audioFile ? <div className="file-row"><div className="file-meta"><FileAudio size={21} /><div><strong>{audioFile.name}</strong><span>{formatBytes(audioFile.size)}</span></div></div><button className="button button-quiet" type="button" onClick={() => setAudioFile(null)}><Trash2 size={16} /> Remove</button></div> : <button className="button button-secondary button-full" type="button" onClick={() => fileInputRef.current?.click()}><Paperclip size={17} /> Upload audio</button>}{error && <div className="notice error" role="alert"><AlertCircle size={17} /> <span>{error}</span></div>}<div className="studio-actions"><button className="button button-primary button-full" type="button" disabled={!audioFile || isRecording} onClick={() => void submitAudio()}>Transcribe with Sahara <ArrowRight size={17} /></button><button className="demo-trigger" type="button" onClick={() => setManualOpen((open) => !open)}>{manualOpen ? "Close text input" : "Use text instead"}</button><button className="demo-trigger" type="button" onClick={() => void loadDemo()}>Open verified-report demo</button></div>{manualOpen && <div className="clarification-card"><label className="field-label" htmlFor="manual-transcript">Text or transcript</label><textarea id="manual-transcript" className="text-area" value={manualTranscript} onChange={(event) => setManualTranscript(event.target.value)} placeholder="Paste something you want to understand or express…" /><button className="button button-dark button-full" type="button" onClick={submitManualTranscript}>Continue <ArrowRight size={17} /></button></div>}<p className="privacy-note"><LockKeyhole size={15} /> Audio is sent only when you continue. Report cases stay in this browser and expire after 24 hours.</p></>}

      {view === "transcript" && readyTranscript && <><p className="section-kicker">What Sahara heard</p><h2>Check the words before Ìròyìn acts.</h2><p className="studio-intro">Sahara&apos;s original stays preserved. Fix a word only if the speech model heard you incorrectly.</p><label className="field-label" htmlFor="transcript-correction">Transcript you want Ìròyìn to use</label><textarea id="transcript-correction" className="text-area" value={transcriptDraft} onChange={(event) => setTranscriptDraft(event.target.value)} /><p className="micro-copy">Source · {readyTranscript.provider === "sahara" ? "Sahara by Intron" : "Text input"}{transcriptChanged ? " · corrected by you" : " · unchanged"}</p>{transcriptChanged && <div className="source-strip"><span>Original Sahara transcript</span><p>{readyTranscript.originalText ?? readyTranscript.text}</p></div>}<div className="task-grid"><button className="task-card" type="button" disabled={!transcriptDraft.trim()} onClick={() => chooseTransform("explain")}><BookOpenText size={20} /><span><strong>Explain</strong><small>Make the meaning easier to understand</small></span><ArrowRight size={17} /></button><button className="task-card" type="button" disabled={!transcriptDraft.trim()} onClick={() => chooseTransform("express")}><Languages size={20} /><span><strong>Express</strong><small>Say the same thought in the form you need</small></span><ArrowRight size={17} /></button><button className="task-card report-task" type="button" disabled={!transcriptDraft.trim()} onClick={() => { const current = currentTranscript(); if (current) void structureTranscript(current); }}><FileCheck2 size={20} /><span><strong>Report</strong><small>Build a verified record for a higher-stakes event</small></span><ArrowRight size={17} /></button></div>{error && <div className="notice error" role="alert"><AlertCircle size={17} /> <span>{error}</span></div>}<button className="demo-trigger restart-link" type="button" onClick={resetCapture}><RotateCcw size={15} /> Start again</button></>}

      {view === "choose-output" && readyTranscript && pendingMode && <><p className="section-kicker">{pendingMode === "explain" ? "How should it make sense?" : "How should it sound?"}</p><h2>{pendingMode === "explain" ? "Choose how you want it explained." : "Choose the form you need."}</h2><p className="studio-intro">The original Sahara transcript stays preserved. Your checked transcript is what Ìròyìn will use.</p><div className="task-grid">{outputOptions.map((option) => <button className="task-card" type="button" key={option.value} onClick={() => void transformTranscript(pendingMode, option.value)}><Languages size={20} /><span><strong>{option.label}</strong><small>{option.help}</small></span><ArrowRight size={17} /></button>)}</div>{error && <div className="notice error" role="alert"><AlertCircle size={17} /> <span>{error}</span></div>}<button className="demo-trigger restart-link" type="button" onClick={() => setView("transcript")}>Back to actions</button></>}

      {view === "result" && readyTranscript && transformResult && <><div className="result-heading"><span className="result-icon"><Check size={18} /></span><div><p className="section-kicker">{transformResult.mode === "explain" ? "Explained" : "Expressed clearly"}</p><h2>{transformResult.mode === "explain" ? "Here’s the meaning." : "Same thought. Clearer form."}</h2></div></div><div className="result-card" data-output-style={transformResult.outputStyle ?? "simple_en"}><p>{transformResult.result}</p></div><div className="source-strip"><span>{transcriptChanged ? "Checked transcript used" : "Original"}</span><p>{transcriptDraft.trim() || readyTranscript.text}</p></div>{transcriptChanged && <div className="source-strip"><span>Original Sahara transcript</span><p>{readyTranscript.originalText ?? readyTranscript.text}</p></div>}<p className="prototype-note">Sahara handles speech recognition; Ìròyìn&apos;s semantic layer handles Explain and Express. The original transcript remains visible for provenance.</p><div className="result-actions"><button className="button button-secondary" type="button" onClick={() => setView("transcript")}>Choose another action</button><button className="button button-primary" type="button" onClick={resetCapture}>Start over</button></div></>}

      {view === "clarify" && caseFile && clarification && <><p className="section-kicker">Verified report</p><h2>One fact needs you.</h2><p className="studio-intro">This is the report path. Ìròyìn will not invent a critical detail.</p><blockquote className="transcript-preview">{caseFile.transcript}</blockquote><div className="clarification-card"><span className="question-label">Missing critical information</span><h3>{clarification.question}</h3><label className="field-label" htmlFor="clarification-answer">Your answer</label><input id="clarification-answer" className="text-input" value={answer} onChange={(event) => setAnswer(event.target.value)} placeholder="Add only the missing detail" autoFocus /><p className="micro-copy">Question {caseFile.clarifications.length + 1} of at most 3</p>{error && <div className="notice error" role="alert"><AlertCircle size={17} /> <span>{error}</span></div>}<button className="button button-primary button-full" type="button" disabled={!answer.trim()} onClick={() => void submitClarification()}>Save detail & continue <ArrowRight size={17} /></button></div></>}

      {view === "clarify" && caseFile && !clarification && <div className="processing"><div><Check size={28} /><h2>All critical facts are covered.</h2><button className="button button-primary" type="button" onClick={() => router.push(`/report/${encodeURIComponent(caseFile.caseId)}/review`)}>Review report <ArrowRight size={17} /></button></div></div>}
    </div>
  </section>;
}
